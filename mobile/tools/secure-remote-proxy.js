const http=require('http'),net=require('net'),fs=require('fs'),path=require('path'),crypto=require('crypto'),{execFileSync}=require('child_process')
const QRCode=require('./npm/node_modules/qrcode-terminal/vendor/QRCode'),QRErrorCorrectLevel=require('./npm/node_modules/qrcode-terminal/vendor/QRCode/QRErrorCorrectLevel')
const root=path.resolve(__dirname,'..'),dataDir=process.env.DSH_REMOTE_DATA_DIR||path.join(root,'data'),authPath=path.join(dataDir,'remote-auth.json'),publicPath=path.join(dataDir,'remote-url.txt'),apkPath=path.join(root,'Android','DeepSeek-Harness-Android.apk'),listenHost='127.0.0.1',listenPort=3081,targetHost='127.0.0.1',targetPort=3080
fs.mkdirSync(dataDir,{recursive:true})
const randomToken=bytes=>crypto.randomBytes(bytes).toString('base64url'),digest=value=>crypto.createHash('sha256').update(value).digest('hex')
let state;try{state=JSON.parse(fs.readFileSync(authPath,'utf8'))}catch{state={}}
function save(){fs.writeFileSync(authPath,JSON.stringify(state,null,2),{mode:0o600});if(process.platform==='win32')try{execFileSync('icacls.exe',[authPath,'/inheritance:r','/grant:r',`${process.env.USERNAME}:F`,'/grant:r','SYSTEM:F'],{windowsHide:true,stdio:'ignore'})}catch{}}
if(!state.pairHash){state.pairCode=randomToken(18);state.pairHash=digest(state.pairCode);state.devices=[];save()}
function cookies(request){return Object.fromEntries(String(request.headers.cookie||'').split(';').map(item=>item.trim().split('=').map(decodeURIComponent)).filter(item=>item.length===2))}
function authorized(request){const token=cookies(request).dsh_remote_device;return Boolean(token&&state.devices.some(device=>crypto.timingSafeEqual(Buffer.from(device.hash),Buffer.from(digest(token)))))}
function html(response,status,body){response.writeHead(status,{'content-type':'text/html; charset=utf-8','cache-control':'no-store','content-security-policy':"default-src 'none'; style-src 'unsafe-inline'; img-src data:; frame-ancestors http://127.0.0.1:3080"});response.end(`<!doctype html><meta name="viewport" content="width=device-width,initial-scale=1"><title>DeepSeek Harness 远程配对</title><style>body{font:16px system-ui;margin:0;min-height:100vh;display:grid;place-items:center;background:#eef9fb;color:#20343a}.card{max-width:560px;margin:24px;padding:28px;border:2px solid #56aabd;border-radius:16px;background:#ffffffe8}code{word-break:break-all}svg{width:min(78vw,360px);display:block;margin:20px auto}button{padding:12px 18px;border:0;border-radius:8px;background:#56aabd;color:white;font-weight:700}</style><div class="card">${body}</div>`)}
function qrSvg(text){const qr=new QRCode(-1,QRErrorCorrectLevel.M);qr.addData(text);qr.make();const count=qr.getModuleCount(),cell=6,margin=4,size=(count+margin*2)*cell;let paths='';for(let row=0;row<count;row++)for(let column=0;column<count;column++)if(qr.isDark(row,column))paths+=`M${(column+margin)*cell} ${(row+margin)*cell}h${cell}v${cell}h-${cell}z`;return `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ${size} ${size}"><rect width="100%" height="100%" fill="white"/><path d="${paths}" fill="#111"/></svg>`}
function remoteBase(request){const forwarded=String(request.headers['x-forwarded-host']||request.headers.host||'');return `${request.headers['x-forwarded-proto']||(forwarded.includes('trycloudflare.com')?'https':'http')}://${forwarded}`}
function proxyHttp(request,response){const headers={...request.headers,host:`${targetHost}:${targetPort}`};delete headers['x-forwarded-host'];if(headers.cookie){const kept=String(headers.cookie).split(';').map(x=>x.trim()).filter(x=>!x.startsWith('dsh_remote_device='));if(kept.length)headers.cookie=kept.join('; ');else delete headers.cookie}const upstream=http.request({host:targetHost,port:targetPort,method:request.method,path:request.url,headers},proxyResponse=>{response.writeHead(proxyResponse.statusCode||502,proxyResponse.headers);proxyResponse.pipe(response)});upstream.on('error',error=>html(response,502,`<h1>电脑端 Harness 未启动</h1><p>${error.message}</p>`));request.pipe(upstream)}
const server=http.createServer((request,response)=>{const url=new URL(request.url,`http://${request.headers.host||'localhost'}`),code=url.searchParams.get('code')||'';if(url.pathname==='/install'){if(code.length!==state.pairCode.length||!crypto.timingSafeEqual(Buffer.from(digest(code)),Buffer.from(state.pairHash)))return html(response,403,'<h1>安装链接无效或已过期</h1>');const base=remoteBase(request),encoded=encodeURIComponent(code),pairUrl=`${base}/pair?code=${encoded}`,appLink=`deepseek-harness://pair?url=${encodeURIComponent(pairUrl)}`;return html(response,200,`<h1>DeepSeek Harness 手机端</h1><p><a href="${base}/download-apk?code=${encoded}"><button>下载 Android APK 1.3.3</button></a></p><p>安装后回到此页面：</p><p><a href="${appLink}"><button>打开 App 并自动配对</button></a></p>`)}if(url.pathname==='/download-apk'){if(code.length!==state.pairCode.length||!crypto.timingSafeEqual(Buffer.from(digest(code)),Buffer.from(state.pairHash))||!fs.existsSync(apkPath))return html(response,404,'<h1>APK 不存在或链接已过期</h1>');const stat=fs.statSync(apkPath);response.writeHead(200,{'content-type':'application/vnd.android.package-archive','content-disposition':'attachment; filename="DeepSeek-Harness-Android.apk"','content-length':String(stat.size),'cache-control':'no-store'});return fs.createReadStream(apkPath).pipe(response)}if(url.pathname==='/pair'){if(code.length!==state.pairCode.length||!crypto.timingSafeEqual(Buffer.from(digest(code)),Buffer.from(state.pairHash)))return html(response,403,'<h1>配对密钥无效或已使用</h1>');const token=randomToken(32);state.devices.push({hash:digest(token),pairedAt:new Date().toISOString()});state.pairCode=randomToken(18);state.pairHash=digest(state.pairCode);save();response.writeHead(302,{location:'/','set-cookie':`dsh_remote_device=${encodeURIComponent(token)}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=31536000`});return response.end()}if(url.pathname==='/remote-pairing'&&request.socket.remoteAddress==='127.0.0.1'){const base=fs.existsSync(publicPath)?fs.readFileSync(publicPath,'utf8').trim():remoteBase(request),link=`${base}/install?code=${encodeURIComponent(state.pairCode)}`;return html(response,200,`<h1>手机安装与远程配对</h1>${qrSvg(link)}<p>手机扫码后下载安装 App，再点击自动配对。</p><p><code>${link}</code></p>`)}if(!authorized(request))return html(response,401,'<h1>此设备尚未配对</h1>');proxyHttp(request,response)})
server.on('upgrade',(request,socket,head)=>{if(!authorized(request))return socket.destroy();const upstream=net.connect(targetPort,targetHost,()=>{let raw=`${request.method} ${request.url} HTTP/${request.httpVersion}\r\n`;for(let i=0;i<request.rawHeaders.length;i+=2){const name=request.rawHeaders[i],value=name.toLowerCase()==='host'?`${targetHost}:${targetPort}`:request.rawHeaders[i+1];raw+=`${name}: ${value}\r\n`}upstream.write(`${raw}\r\n`);if(head.length)upstream.write(head);socket.pipe(upstream).pipe(socket)});upstream.on('error',()=>socket.destroy())})
server.listen(listenPort,listenHost)
